name: Support PR lifecycle post-5B2 reconstruction

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  reconstruct-and-verify:
    if: github.head_ref == 'support/pr-lifecycle-post-5b2-import-20260805'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv

      - name: Apply clean-runner entrypoint correction
        run: |
          set -euo pipefail
          python scripts/support/apply_pr_lifecycle_post_5b2.py
          python -m py_compile \
            newsroom/checks/pr_lifecycle.py \
            newsroom/tests/test_pr_lifecycle.py \
            scripts/sdlc/pr_lifecycle.py
          git diff --check
          python - <<'PY'
          import subprocess

          expected = {
              '.github/workflows/pr-lifecycle.yml',
              'newsroom/tests/test_pr_lifecycle.py',
              'scripts/sdlc/pr_lifecycle.py',
          }
          lines = subprocess.run(
              ['git', 'status', '--porcelain=v1', '--untracked-files=all'],
              check=True,
              capture_output=True,
              text=True,
          ).stdout.splitlines()
          actual = {line[3:] for line in lines}
          if actual != expected:
              raise SystemExit(
                  f'PR lifecycle correction inventory differs: '
                  f'expected={sorted(expected)!r} actual={sorted(actual)!r}'
              )
          PY

      - name: Commit corrected product files
        run: |
          set -euo pipefail
          git add \
            .github/workflows/pr-lifecycle.yml \
            newsroom/tests/test_pr_lifecycle.py \
            scripts/sdlc/pr_lifecycle.py
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'fix: isolate trusted PR lifecycle contract loading'

      - name: Remove disposable support machinery
        run: |
          set -euo pipefail
          git rm \
            scripts/support/apply_pr_lifecycle_post_5b2.py \
            .github/workflows/support-pr-lifecycle-post-5b2.yml
          git diff --cached --check
          git commit -m 'support: retain product-only PR lifecycle framework'
          test -z "$(git status --porcelain)"

      - name: Sync locked development environment
        run: uv sync --dev --locked

      - name: Focused PR lifecycle evidence
        run: uv run python -m pytest -q newsroom/tests/test_pr_lifecycle.py

      - name: Complete deterministic repository evidence
        run: uv run python -m pytest -q

      - name: Existing clustering evaluation gate
        run: |
          uv run python scripts/eval_clustering_metrics.py \
            --dataset newsroom/evals/clustering_eval_dataset_v1.jsonl \
            --baseline newsroom/evals/clustering_eval_metrics_baseline_v1.json \
            --fail-on-regression

      - name: Publish verified product-only head
        env:
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          test -z "$(git status --porcelain)"
          git push origin "HEAD:${HEAD_BRANCH}"
