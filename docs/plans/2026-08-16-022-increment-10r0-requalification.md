# Increment 10R0 canary entry-gate requalification

- **Issue:** [#526](https://github.com/fol2/newsroom/issues/526)
- **Parent:** [#150](https://github.com/fol2/newsroom/issues/150)
- **Packet:** `newsroom/increment10/requalification_v1.json`
- **Module:** `newsroom.increment10.requalification`
- **Outcome:** `ELIGIBLE_FOR_INCREMENT10_PLAN`

## Upstream truth

The packet preserves the signed Increment 9 exact-main outcome without
reinterpretation:

- `main@bd9ce46262a9286080e0dc5d648e33f94a9c6178`;
- tree `1b3f69d305539432937ce5fdd8242bea83a1d659`;
- Tier-M run `31927978494`;
- closeout digest
  `sha256:a9eb84e3513b6a71c0d3301024c943aa75e8f112cec22c774ee4197bdd5e566b`;
- disposition `BLOCKED_ACTIVE_COVERAGE`; and
- Increment 10 eligibility false on that upstream authority line.

The retained missing-evidence state is not turned into a shadow PASS. All ten
runtime gates and ten source-rights gates remain explicitly `MISSING`; all
twelve zero-tolerance results remain `NOT_EVALUATED`.

## Requalification scope

The owner activation permits one narrower plan only:

- three exact repository fixture Candidate Version identities;
- destination `local://increment10/evidence-intake-fixture-v1`;
- decision-bearing scope limited to local transport correctness, local intake
  acknowledgement, reconciliation and control evidence;
- no live source bytes or external evidence acquisition;
- no provider, model, embedding or live reviewer call;
- no credential class, secret location, non-loopback egress or spend; and
- no publication, public dispatch, production authority or production mutation.

The current `FIXTURE_OPERATIONAL_ADMITTED` result is compatible with planning
this local fixture canary only. Its exact admission, schema, migration,
Operational Profile and non-effect identities must be rechecked at plan freeze,
controller qualification and runtime admission. Drift blocks.

## Prospective closure of the retained gap

The packet classifies all twenty residual gates across owner decisions,
rights/licences, credentials/egress, budget, Operational Admission and technical
readiness. Each gate has one downstream issue owner and a prospective evidence
path. Each zero-tolerance finding has a pre-registered injection or review path
and required observed count zero.

The ten stable `EINT-FX-*` requirements explicitly authorise only the minimum
local fixture Evidence Intake contract: exact correlation, fixture rights,
Observation/Package separation, sufficiency separation, synthetic sensitive
content, retention/deletion, complete outcomes and feedback, no external effect,
prospective canary control, and discovery-not-evidence.

## Decision semantics

`ELIGIBLE_FOR_INCREMENT10_PLAN` means that #527 may construct an immutable plan
after this packet closes with signed evidence. It does not authorise
implementation, Evidence Intake, a decision-bearing canary or any external or
public effect. Runtime remains separately gated through #529–#534 and an exact
#533 execution-authority record.

The packet's `permits_increment10_plan` property therefore remains false. A
manual exact-main Tier-M run must independently reconstruct the retained packet
from the evaluated checkout and include it in the OIDC/Sigstore subject bundle.
Only that verified signed closeout plus closed #526 may activate #527.

`REMAIN_BLOCKED` and `RETURN_TO_BOUNDED_SHADOW` remain valid closed alternatives
for any future superseding requalification. Missing or not-evaluated evidence
never becomes PASS by default.
