# Corrected Newsroom RAM/CPU packet (#898)

- Role: Dated provider-free research evidence
- Status: Measured on the intended Mini
- R1 retained-observation body-scan: `FEASIBILITY_GO`
- First migration atom / R2 bounded event resolver: `HOLD`
- Owner: fol2
- Canonical language: English
- Date: 2026-09-02
- Ticket: [#898](https://github.com/fol2/newsroom/issues/898)
- Execution head: `2b82e8b2ca7918a41ed5b837f073eab6f7e34994`
- Canonical measurements: [`2026-09-02-issue-898-ram-cpu-measurements.json`](2026-09-02-issue-898-ram-cpu-measurements.json)

This note is non-normative. It authorises no product-code change, no Python
exact-row-selection fix, no production Rust crate, no runtime integration,
and no implementation issue.

The earlier Mini `NO_GO` is withdrawn. It was not a valid Rust decision:
the one-event child scanned twice, primary runs enabled `tracemalloc`, the
SQLite copy was not a backup-API snapshot, and no Rust comparator was
measured.

## Decision

```text
R1 FEASIBILITY_GO —
Rust materially reduces retained-observation scan RSS.

FIRST MIGRATION ATOM —
HOLD pending a bounded useful output boundary.
```

Python and research-only Rust processed the same 6,450 HTTP-200 rows
(613,281,383 body bytes) from a consistent copied proving snapshot and
emitted the same manifest digest
`sha256:ec31a8baf6bc5a58204ed4551fd58a0fa896bc55b035f5dd90a49c663d97d0cb`.

After launch and IPC, **peak** RSS falls from 661.8 MiB (Python scan
process) to 526.3 MiB (Python parent plus Rust child), a 135.5 MiB /
20.5% reduction. That clears both the 64 MiB and 20% gates. The Rust
child itself peaks at 482.7 MiB.

CPU for the machine gate is Rust child plus e2e parent, not the parent
alone: 1.55 s + 0.065 s = 1.61 s versus Python 1.37 s (about 18%
higher, under the 20% hold). The GO is based on **peak RSS**. Rust e2e
retained 21.0 MiB is dominated by child-process termination and is not
a long-lived Rust worker claim.

This does **not** claim event-resolution unit parity and is not a
migration green light. One-event Python resolution still reconstructs
1,233 units and peaks at 864.7 MiB. R2 remains `HOLD`:
`proving_observations` has no `item_key` / `revision_digest` /
`published_at` / `updated_at` columns, so a bounded one-event SQL
candidate cannot be supplied without a product schema or query change,
which #898 forbids.

`FEASIBILITY_GO` authorises no implementation.

## Hardware and method

Host: Mac16,10, Apple M4, 10 cores, 16 GiB. Fresh child process per run.
One warm-up and three measured runs except the process-tree snapshot,
R2, and supplementary `tracemalloc` cases. Peak RSS is the maximum of
`/usr/bin/time -l`, `resource.getrusage`, in-process held RSS and, for
the Rust subprocess, combined parent-plus-child `ps` samples. Primary
runs have `tracemalloc` off. The copied store was created with
`sqlite3.Connection.backup` from a read-only source (17,690
observations, 1,686,304,566 body bytes, 1,789,808,640 snapshot bytes,
page size 4,096, WAL). Event identity for one-event cases was prepared
outside the measured child. No daemon was signalled. No queue event was
claimed.

Research Rust: `docs/research/issue-898-ram-cpu-rust`, `rustc` 1.98.0,
`--release`, not a product crate.

## Ranked peaks

| Case | Peak RSS | Retained RSS | Median CPU |
|---|---:|---:|---:|
| B10 corrected one-event resolve on copied store | 864.7 MiB | 548.8 MiB | 47.5 s |
| E1 copied full-corpus load | 859.3 MiB | 497.0 MiB | 54.9 s |
| D1 copied-store `max_writes=0` cycle | 838.8 MiB | 471.5 MiB | 51.5 s |
| R1 Python observation scan | 661.8 MiB | 646.1 MiB | 1.37 s |
| R1 Rust end-to-end peak (parent + child RSS) | 526.3 MiB | 21.0 MiB† | 1.61 s‡ |
| R1 Rust child scan | 482.7 MiB | 230.0 MiB | 1.55 s |
| S1 import cycle with `tracemalloc` | 91.2 MiB | 85.5 MiB | 11.8 s |
| A4 idle Graphiti worker | 86.9 MiB | 86.8 MiB | 0.78 s |
| A2 import `cycle` (`tracemalloc` off) | 76.5 MiB | 76.4 MiB | 0.58 s |
| A1 bare interpreter | 19.0 MiB | 18.9 MiB | 0.00 s |
| R0 Rust process baseline | 1.7 MiB | 1.7 MiB | 0.00 s |
| Live Neo4j (read-only `ps`) | 17.0 MiB | 17.0 MiB | — |

† Rust e2e retained 21.0 MiB is dominated by child-process termination.
The R1 feasibility GO is based on peak RSS, not that post-exit parent
figure.

‡ Machine CPU gate uses Rust child 1.55 s plus e2e parent 0.065 s.

Fixture one-event work still does not explain production RAM: solo
77.8 MiB, 10× tiny rows 77.3 MiB, 40× 8 KiB rows 77.8 MiB. The copied
7-day corpus does: one event kept **1** of **1,233** units. Cycle D1
formed **280** candidates from **6,450** poll observations, minted **0**,
Graphiti **0**.

Malformed and empty inputs returned zero selected units and did not
claim a queue event.

## Rust estimate

- R0 idle research binary: 1.7 MiB.
- R1 same-boundary scan: exact row-manifest parity; peak-RSS gate
  cleared after IPC; child-plus-parent CPU under the 20% hold.
- R2 bounded one-event candidate: `HOLD`. Missing seam is exact event
  identity columns, or an existing exact-row selection API, on the
  proving snapshot. #898 does not authorise adding that seam.

R1 proves large-BLOB ownership and hashing is cheaper in a Rust child.
It does not prove that moving the Graphiti event resolver to Rust would
keep exact unit semantics or remove the 864.7 MiB one-event peak.

Supplementary `tracemalloc` on import costs about 15 MiB and 11 s of
CPU versus the primary import. That contamination is why the withdrawn
packet cannot be used as a Rust decision.

## Graphiti Research lane

Exact-head Graphiti Research failed at
`test_graphiti_combined_temporal_runtime.py::test_ambiguous_hold_does_not_mint_or_guess_relation_endpoint`
(`IndexError: tuple index out of range`; 1 failed, 284 passed).

The same command on exact base `a67871d28164147a049670f71ce155afefb62a02`
fails the same test with the same traceback (1 failed, 284 passed).
This is an exact-base pre-existing unrelated failure. #898 does not
change product code and does not repair it.

## Non-effects

No product file was changed. No implementation issue was opened.
