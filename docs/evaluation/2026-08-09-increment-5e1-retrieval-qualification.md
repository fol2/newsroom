# Increment 5E1 retrieval qualification

## Boundary

This is the repository-owned, no-public-effect qualification for issue #332. `HYBRID` is the only decision-bearing target. `EXACT_ONLY`, `FULL_TEXT_ONLY`, `VECTOR_ONLY`, and `ADMITTED_GRAPH_ONLY` are separately reported comparative ablations and cannot rescue a failed hybrid result.

The canonical production-shaped target (`sha256:fd88328d9ed74573a9b0aa2e7900dc437a7f8a4963c96da59869b9dddbad7271`) binds Neo4j Community `2026.06.0`, Python driver `6.2.0`, the accepted Increment 5A contract, all ten component digests, the reviewed evaluation plan, and one exact Git tree. Graphiti remains a disabled, unqualified proposal seam. Vector evidence is deterministic fixed-point fixture/replay only and makes no production embedding-quality claim.

No live source, provider, model, embedding call, protected source expression, credential, external egress, spend, Candidate or Hypothesis mutation, publication, qualification authority, or production activation is introduced.

## Corpus

The canonical corpus specification (`sha256:07f5f0cceb26c0744aadd6feacb056aa09503b0552ec1223a4f3228af6fbeb70`) generates exactly 100 repository-owned synthetic qualification cases:

- 30 event/development precision cases;
- 30 source-revision impact cases; and
- 40 long-running policy, case, or process timeline cases.

Each mandatory case type appears ten times. Cases cycle through EN-GB, ZH-Hant-HK, and mixed EN-GB/ZH-Hant-HK labels. The corpus also covers correction and supersession, distractor false merge, rights purge and rebuild, shared-origin dependency, long-running timelines, and temporal cutoff. Label digests and branch-result fixture digests are retained separately for every case, while the complete generated dataset manifest binds both.

## Evidence semantics

One Epoch binds the target manifest, accepted component identities, exact code tree, generation, dataset and query manifests, label policy, thresholds, and evaluation policies. Every case requires one observation from each of the five systems, for 500 exact observations.

Missing, duplicate, unexpected, or under-exposed evidence is `NOT_EVALUATED`, never PASS. The HYBRID target applies the frozen aggregate, mandatory-family, required-slice, triage, latency, provenance, trust, temporal, rights, no-false-success, scope, write, and rebuild gates. Non-automatic triage classes remain reported with counts and denominators without inventing post-hoc thresholds.

Reports are canonical and content-addressed. A first-writer-wins SQLite journal retains PASS, FAIL, and NOT_EVALUATED outcomes and rejects Epoch reuse, retained-byte corruption, and deterministic replay mismatch.

## Actual-service lane

The permanent Projection B2/B3/C1 Neo4j workflow automatically selects `test_projection_b2_increment5e1_qualification.py`. The test authenticates to the disposable Neo4j service, verifies the exact server edition/version and Python driver against the frozen target, binds the workflow Git tree into the Epoch, then evaluates all 500 repository fixture observations under that exact identity. It emits the Epoch digest, report digest, and complete canonical report into JUnit evidence. The service check proves the selected target identity; it does not claim that every synthetic fixture result was produced by a live graph query.

## Deferred ownership

Prospective production event universes, human reviewers, blinding, adjudication, release-evidence authority, Operational Profiles, schedules, queues, observability, complete backup/restore, capacity, licence, and Operational Admission remain Increment 8 / #148. Cross-request Hypothesis, Candidate, and Handoff effects remain Increment 6 / #146.
