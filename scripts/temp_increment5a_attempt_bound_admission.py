from __future__ import annotations

import hashlib
import json
from pathlib import Path
import subprocess

ROOT = Path(__file__).resolve().parents[1]
OLD_APPROVAL_SCHEMA_DIGEST = "sha256:8e69e5a66949e103ec4a960af34a509f3ba8b86a9f32b08ed77930f0898b8577"
OLD_OWNER_BODY_DIGEST = "sha256:600c6b43c645d8c26d234c7a29d325650ab164791cdde457ac96b55b1b99cacd"
OLD_MAIN_SCHEMA_DIGEST = "sha256:d8548042a581396f30ef522df7f4c95ab03f1298ef53b773a433d2eae03cbe88"
NEW_APPROVAL_EFFECT = "PRODUCTION_EQUIVALENT_QUALIFICATION_ONLY"
MAIN_CODE = 'from __future__ import annotations\n\nfrom copy import deepcopy\nfrom dataclasses import dataclass\nimport json\nfrom pathlib import Path\nimport re\nfrom typing import Any, Mapping\n\nfrom jsonschema import Draft202012Validator\n\nfrom newsroom.authority.canonical import (\n    canonical_json_bytes,\n    digest_bytes,\n    validate_sha256_digest,\n)\nfrom newsroom.authority.types import UtcTimestamp\n\nfrom .contracts import (\n    Increment5ADecisionPacket,\n    Increment5ContractError,\n)\nfrom .decision import INCREMENT_5A_DECISION_PACKET\nfrom .decision_validation import object_without_duplicate_names\n\n\nMAIN_QUALIFICATION_RECORD_SCHEMA_ID = (\n    "urn:newsroom:increment5a:main-qualification-record:v2"\n)\nMAIN_QUALIFICATION_RECORD_SCHEMA_VERSION = (\n    "increment5a-main-qualification-record-v2"\n)\nMAIN_QUALIFICATION_RECORD_ID = "increment5a-post-merge-main-qualification"\nMAIN_QUALIFICATION_RECORD_VERSION = "increment5a-main-qualification-v2"\nMAIN_QUALIFICATION_SCOPE = "POST_MERGE_INCREMENT5_IMPLEMENTATION_ADMISSION"\nMAIN_QUALIFICATION_EFFECT = "IMPLEMENTATION_OF_ISSUES_251_254_ONLY"\nMAIN_QUALIFICATION_NON_EFFECTS = (\n    "CANARY",\n    "EXTERNAL_EMBEDDING_API_CALLS",\n    "LIVE_SOURCE_EXECUTION",\n    "PROTECTED_CONTENT_VECTORS",\n    "PROVIDER_SPENDING",\n    "PUBLICATION",\n    "PUBLIC_EFFECT",\n    "PRODUCTION_ACTIVATION",\n    "SHADOW",\n)\nMAIN_QUALIFICATION_REPOSITORY = "fol2/newsroom"\nMAIN_QUALIFICATION_REPOSITORY_ID = 1153895518\nMAIN_QUALIFICATION_BRANCH = "main"\nMAIN_QUALIFICATION_REF = "refs/heads/main"\nMAIN_QUALIFICATION_ISSUE_NUMBER = 250\nMAIN_QUALIFICATION_PULL_REQUEST_NUMBER = 255\nMAIN_QUALIFICATION_WORKFLOW_SPECS: dict[str, tuple[int, str]] = {\n    "CI": (232327316, "CI"),\n    "AUTHORITY_A2A": (315268483, "Authority A2a"),\n    "AUTHORITY_A2B": (315287552, "Authority A2b"),\n    "PROJECTION_B1": (317445524, "Projection B1"),\n    "PROJECTION_B2_B3_C1_NEO4J": (\n        317681630,\n        "Projection B2/B3/C1 Neo4j",\n    ),\n    "SDLC_EVIDENCE_SHADOW": (318982302, "SDLC Evidence Shadow"),\n}\nMAIN_QUALIFICATION_WORKFLOW_NAMES = tuple(MAIN_QUALIFICATION_WORKFLOW_SPECS)\nMAIN_QUALIFICATION_RECORD_SCHEMA_PATH = (\n    Path(__file__).resolve().parent\n    / "data"\n    / "increment5a_main_qualification_record_v2.schema.json"\n)\nMAIN_QUALIFICATION_RECORD_PATH = (\n    Path(__file__).resolve().parent\n    / "data"\n    / "increment5a_main_qualification_record_v2.json"\n)\n# A later post-merge admission commit replaces None with the exact canonical\n# record digest after the merged 5A commit has passed exact-main qualification.\nMAIN_QUALIFICATION_RECORD_DIGEST: str | None = None\n_SHA256_PATTERN = r"^sha256:[0-9a-f]{64}$"\n_COMMIT_PATTERN = r"^[0-9a-f]{40}$"\n\n\ndef _workflow_attempt_schema(\n    *,\n    workflow_id: int,\n    workflow_name: str,\n) -> dict[str, object]:\n    return {\n        "type": "object",\n        "required": [\n            "workflow_id",\n            "workflow_name",\n            "run_id",\n            "run_attempt",\n            "run_number",\n            "repository",\n            "repository_id",\n            "event",\n            "ref",\n            "head_branch",\n            "head_sha",\n            "head_tree_sha",\n            "workflow_sha",\n            "workflow_ref",\n            "status",\n            "conclusion",\n            "api_url",\n            "html_url",\n            "created_at",\n            "run_started_at",\n            "updated_at",\n        ],\n        "additionalProperties": False,\n        "properties": {\n            "workflow_id": {"const": workflow_id},\n            "workflow_name": {"const": workflow_name},\n            "run_id": {"type": "integer", "minimum": 1},\n            "run_attempt": {"type": "integer", "minimum": 1},\n            "run_number": {"type": "integer", "minimum": 1},\n            "repository": {"const": MAIN_QUALIFICATION_REPOSITORY},\n            "repository_id": {"const": MAIN_QUALIFICATION_REPOSITORY_ID},\n            "event": {"const": "push"},\n            "ref": {"const": MAIN_QUALIFICATION_REF},\n            "head_branch": {"const": MAIN_QUALIFICATION_BRANCH},\n            "head_sha": {"type": "string", "pattern": _COMMIT_PATTERN},\n            "head_tree_sha": {\n                "type": "string",\n                "pattern": _COMMIT_PATTERN,\n            },\n            "workflow_sha": {"type": "string", "pattern": _COMMIT_PATTERN},\n            "workflow_ref": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 512,\n            },\n            "status": {"const": "completed"},\n            "conclusion": {"const": "success"},\n            "api_url": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 512,\n            },\n            "html_url": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 512,\n            },\n            "created_at": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 64,\n            },\n            "run_started_at": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 64,\n            },\n            "updated_at": {\n                "type": "string",\n                "minLength": 1,\n                "maxLength": 64,\n            },\n        },\n    }\n\n\nMAIN_QUALIFICATION_RECORD_SCHEMA: dict[str, object] = {\n    "$schema": "https://json-schema.org/draft/2020-12/schema",\n    "$id": MAIN_QUALIFICATION_RECORD_SCHEMA_ID,\n    "type": "object",\n    "required": [\n        "schema_version",\n        "qualification_id",\n        "qualification_version",\n        "qualification_scope",\n        "qualification_effect",\n        "non_effects",\n        "repository",\n        "branch",\n        "issue_number",\n        "pull_request_number",\n        "qualified_main_commit_sha",\n        "qualified_main_tree_sha",\n        "qualified_main_commit_url",\n        "qualified_at",\n        "approval_record_digest",\n        "proposal",\n        "workflow_attempts",\n        "signed_decision",\n    ],\n    "additionalProperties": False,\n    "properties": {\n        "schema_version": {\n            "const": MAIN_QUALIFICATION_RECORD_SCHEMA_VERSION\n        },\n        "qualification_id": {"const": MAIN_QUALIFICATION_RECORD_ID},\n        "qualification_version": {\n            "const": MAIN_QUALIFICATION_RECORD_VERSION\n        },\n        "qualification_scope": {"const": MAIN_QUALIFICATION_SCOPE},\n        "qualification_effect": {"const": MAIN_QUALIFICATION_EFFECT},\n        "non_effects": {"const": list(MAIN_QUALIFICATION_NON_EFFECTS)},\n        "repository": {"const": MAIN_QUALIFICATION_REPOSITORY},\n        "branch": {"const": MAIN_QUALIFICATION_BRANCH},\n        "issue_number": {"const": MAIN_QUALIFICATION_ISSUE_NUMBER},\n        "pull_request_number": {\n            "const": MAIN_QUALIFICATION_PULL_REQUEST_NUMBER\n        },\n        "qualified_main_commit_sha": {\n            "type": "string",\n            "pattern": _COMMIT_PATTERN,\n        },\n        "qualified_main_tree_sha": {\n            "type": "string",\n            "pattern": _COMMIT_PATTERN,\n        },\n        "qualified_main_commit_url": {\n            "type": "string",\n            "minLength": 1,\n            "maxLength": 512,\n        },\n        "qualified_at": {\n            "type": "string",\n            "minLength": 1,\n            "maxLength": 64,\n        },\n        "approval_record_digest": {\n            "type": "string",\n            "pattern": _SHA256_PATTERN,\n        },\n        "proposal": {\n            "type": "object",\n            "required": [\n                "payload_digest",\n                "record_digest",\n                "contract_bundle_digest",\n                "qualification_profile_schema_digest",\n                "main_qualification_record_schema_digest",\n            ],\n            "additionalProperties": False,\n            "properties": {\n                "payload_digest": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n                "record_digest": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n                "contract_bundle_digest": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n                "qualification_profile_schema_digest": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n                "main_qualification_record_schema_digest": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n            },\n        },\n        "workflow_attempts": {\n            "type": "object",\n            "required": list(MAIN_QUALIFICATION_WORKFLOW_NAMES),\n            "additionalProperties": False,\n            "properties": {\n                name: _workflow_attempt_schema(\n                    workflow_id=workflow_id,\n                    workflow_name=workflow_name,\n                )\n                for name, (\n                    workflow_id,\n                    workflow_name,\n                ) in MAIN_QUALIFICATION_WORKFLOW_SPECS.items()\n            },\n        },\n        "signed_decision": {\n            "type": "object",\n            "required": [\n                "workflow_run_id",\n                "workflow_run_attempt",\n                "decision_document_digest",\n                "decision_identity",\n                "result",\n                "result_reason",\n                "evaluated_sha",\n                "evaluated_tree_sha",\n                "test_count",\n                "skip_count",\n                "required_skip_count",\n                "failure_count",\n                "error_count",\n                "decision_document",\n            ],\n            "additionalProperties": False,\n            "properties": {\n                "workflow_run_id": {\n                    "type": "integer",\n                    "minimum": 1,\n                },\n                "workflow_run_attempt": {\n                    "type": "integer",\n                    "minimum": 1,\n                },\n                "decision_document_digest": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n                "decision_identity": {\n                    "type": "string",\n                    "pattern": _SHA256_PATTERN,\n                },\n                "result": {"const": "PASS"},\n                "result_reason": {"const": "PASS:decision"},\n                "evaluated_sha": {\n                    "type": "string",\n                    "pattern": _COMMIT_PATTERN,\n                },\n                "evaluated_tree_sha": {\n                    "type": "string",\n                    "pattern": _COMMIT_PATTERN,\n                },\n                "test_count": {\n                    "type": "integer",\n                    "minimum": 1,\n                },\n                "skip_count": {\n                    "type": "integer",\n                    "minimum": 0,\n                },\n                "required_skip_count": {"const": 0},\n                "failure_count": {"const": 0},\n                "error_count": {"const": 0},\n                "decision_document": {"type": "object"},\n            },\n        },\n    },\n}\nDraft202012Validator.check_schema(MAIN_QUALIFICATION_RECORD_SCHEMA)\n_MAIN_QUALIFICATION_VALIDATOR = Draft202012Validator(\n    MAIN_QUALIFICATION_RECORD_SCHEMA\n)\nMAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST = digest_bytes(\n    canonical_json_bytes(MAIN_QUALIFICATION_RECORD_SCHEMA)\n)\n\n\ndef _require_schema_artifact() -> None:\n    try:\n        data = MAIN_QUALIFICATION_RECORD_SCHEMA_PATH.read_bytes()\n        value = json.loads(\n            data.decode("utf-8", errors="strict"),\n            object_pairs_hook=object_without_duplicate_names,\n        )\n    except (OSError, UnicodeError, json.JSONDecodeError) as exc:\n        raise Increment5ContractError(\n            "cannot load Increment 5A main-qualification schema artifact"\n        ) from exc\n    if not isinstance(value, dict):\n        raise Increment5ContractError(\n            "main-qualification schema artifact must be an object"\n        )\n    if (\n        data != canonical_json_bytes(value)\n        or value != MAIN_QUALIFICATION_RECORD_SCHEMA\n        or digest_bytes(data) != MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST\n    ):\n        raise Increment5ContractError(\n            "main-qualification schema artifact differs from repository code"\n        )\n\n\n# MATERIALIZE_SCHEMA_CHECK\n\n\ndef _qualification_schema_digest() -> str:\n    from .profiles import QUALIFICATION_PRODUCTION_PROFILE_SCHEMA_DIGEST\n\n    return QUALIFICATION_PRODUCTION_PROFILE_SCHEMA_DIGEST\n\n\ndef _parse_canonical_utc(value: object, *, field: str) -> UtcTimestamp:\n    if not isinstance(value, str):\n        raise Increment5ContractError(f"{field} is not valid UTC text")\n    try:\n        parsed = UtcTimestamp.parse(value)\n    except (TypeError, ValueError) as exc:\n        raise Increment5ContractError(f"{field} is not valid UTC text") from exc\n    if value != parsed.to_text():\n        raise Increment5ContractError(f"{field} must be canonical UTC text")\n    return parsed\n\n\ndef _require_commit_sha(value: object, *, field: str) -> str:\n    if not isinstance(value, str) or re.fullmatch(_COMMIT_PATTERN, value) is None:\n        raise Increment5ContractError(\n            f"{field} must be lowercase 40-hex text"\n        )\n    if value == "0" * 40:\n        raise Increment5ContractError(f"{field} cannot be the null commit")\n    return value\n\n\ndef _require_digest(value: object, *, field: str) -> str:\n    if not isinstance(value, str):\n        raise Increment5ContractError(f"{field} must be a digest")\n    try:\n        return validate_sha256_digest(value, field=field)\n    except ValueError as exc:\n        raise Increment5ContractError(f"{field} is not canonical") from exc\n\n\ndef _require_positive_integer(value: object, *, field: str) -> int:\n    if isinstance(value, bool) or not isinstance(value, int) or value <= 0:\n        raise Increment5ContractError(f"{field} must be a positive integer")\n    return value\n\n\ndef _require_nonnegative_integer(value: object, *, field: str) -> int:\n    if isinstance(value, bool) or not isinstance(value, int) or value < 0:\n        raise Increment5ContractError(\n            f"{field} must be a nonnegative integer"\n        )\n    return value\n\n\ndef _schema_errors(value: Mapping[str, Any]) -> tuple[str, ...]:\n    errors = sorted(\n        _MAIN_QUALIFICATION_VALIDATOR.iter_errors(value),\n        key=lambda item: [str(component) for component in item.absolute_path],\n    )\n    return tuple(\n        (\n            "/".join(str(component) for component in error.absolute_path)\n            or "<root>"\n        )\n        + ": "\n        + error.message\n        for error in errors\n    )\n\n\n@dataclass(frozen=True, slots=True)\nclass WorkflowAttemptEvidence:\n    key: str\n    workflow_id: int\n    workflow_name: str\n    run_id: int\n    run_attempt: int\n    run_number: int\n    head_sha: str\n    head_tree_sha: str\n    workflow_sha: str\n    workflow_ref: str\n    api_url: str\n    html_url: str\n    created_at: UtcTimestamp\n    run_started_at: UtcTimestamp\n    updated_at: UtcTimestamp\n\n    def __post_init__(self) -> None:\n        expected = MAIN_QUALIFICATION_WORKFLOW_SPECS.get(self.key)\n        if expected != (self.workflow_id, self.workflow_name):\n            raise Increment5ContractError(\n                "workflow attempt identity differs from permanent workflow"\n            )\n        for field_name in ("run_id", "run_attempt", "run_number"):\n            _require_positive_integer(\n                getattr(self, field_name),\n                field=f"workflow_attempt.{field_name}",\n            )\n        for field_name in ("head_sha", "head_tree_sha", "workflow_sha"):\n            _require_commit_sha(\n                getattr(self, field_name),\n                field=f"workflow_attempt.{field_name}",\n            )\n        expected_api_url = (\n            f"https://api.github.com/repos/{MAIN_QUALIFICATION_REPOSITORY}/"\n            f"actions/runs/{self.run_id}/attempts/{self.run_attempt}"\n        )\n        expected_html_url = (\n            f"https://github.com/{MAIN_QUALIFICATION_REPOSITORY}/"\n            f"actions/runs/{self.run_id}"\n        )\n        if self.api_url != expected_api_url:\n            raise Increment5ContractError(\n                "workflow attempt API URL differs"\n            )\n        if self.html_url != expected_html_url:\n            raise Increment5ContractError(\n                "workflow attempt HTML URL differs"\n            )\n        if not (\n            self.workflow_ref.startswith(\n                f"{MAIN_QUALIFICATION_REPOSITORY}/"\n            )\n            and self.workflow_ref.endswith(f"@{MAIN_QUALIFICATION_REF}")\n        ):\n            raise Increment5ContractError(\n                "workflow ref is not exact main"\n            )\n        if self.created_at.value > self.run_started_at.value:\n            raise Increment5ContractError(\n                "workflow attempt starts before creation"\n            )\n        if self.run_started_at.value > self.updated_at.value:\n            raise Increment5ContractError(\n                "workflow attempt completes before start"\n            )\n\n    @property\n    def identity(self) -> tuple[int, int]:\n        return self.run_id, self.run_attempt\n\n\n@dataclass(frozen=True, slots=True)\nclass Increment5AMainQualificationRecord:\n    qualified_main_commit_sha: str\n    qualified_main_tree_sha: str\n    qualified_at: UtcTimestamp\n    approval_record_digest: str\n    proposal_payload_digest: str\n    proposal_record_digest: str\n    proposal_contract_bundle_digest: str\n    qualification_profile_schema_digest: str\n    main_qualification_record_schema_digest: str\n    workflow_attempts: tuple[WorkflowAttemptEvidence, ...]\n    decision_document_digest: str\n    decision_identity: str\n    test_count: int\n    skip_count: int\n    record_digest: str\n\n    def __post_init__(self) -> None:\n        _require_commit_sha(\n            self.qualified_main_commit_sha,\n            field="qualified_main_commit_sha",\n        )\n        _require_commit_sha(\n            self.qualified_main_tree_sha,\n            field="qualified_main_tree_sha",\n        )\n        if not isinstance(self.qualified_at, UtcTimestamp):\n            raise Increment5ContractError(\n                "main qualification time must be typed UTC"\n            )\n        for field_name in (\n            "approval_record_digest",\n            "proposal_payload_digest",\n            "proposal_record_digest",\n            "proposal_contract_bundle_digest",\n            "qualification_profile_schema_digest",\n            "main_qualification_record_schema_digest",\n            "decision_document_digest",\n            "decision_identity",\n            "record_digest",\n        ):\n            _require_digest(getattr(self, field_name), field=field_name)\n        if (\n            self.main_qualification_record_schema_digest\n            != MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST\n        ):\n            raise Increment5ContractError(\n                "main qualification schema digest differs"\n            )\n        actual_names = tuple(item.key for item in self.workflow_attempts)\n        if actual_names != MAIN_QUALIFICATION_WORKFLOW_NAMES:\n            raise Increment5ContractError(\n                "main qualification workflow inventory differs"\n            )\n        identities = [item.identity for item in self.workflow_attempts]\n        if len(set(identities)) != len(identities):\n            raise Increment5ContractError(\n                "workflow attempt identities must be distinct"\n            )\n        for attempt in self.workflow_attempts:\n            if (\n                attempt.head_sha != self.qualified_main_commit_sha\n                or attempt.workflow_sha != self.qualified_main_commit_sha\n                or attempt.head_tree_sha != self.qualified_main_tree_sha\n            ):\n                raise Increment5ContractError(\n                    "workflow attempt is not bound to qualified main"\n                )\n        _require_positive_integer(self.test_count, field="test_count")\n        _require_nonnegative_integer(self.skip_count, field="skip_count")\n\n    @property\n    def workflow_attempt_by_name(self) -> dict[str, WorkflowAttemptEvidence]:\n        return {item.key: item for item in self.workflow_attempts}\n\n    @property\n    def qualified_main_commit_url(self) -> str:\n        return (\n            f"https://github.com/{MAIN_QUALIFICATION_REPOSITORY}/commit/"\n            f"{self.qualified_main_commit_sha}"\n        )\n\n\ndef _parse_workflow_attempt(\n    *,\n    key: str,\n    value: Mapping[str, Any],\n    qualified_main_commit_sha: str,\n    qualified_main_tree_sha: str,\n) -> WorkflowAttemptEvidence:\n    workflow_id, workflow_name = MAIN_QUALIFICATION_WORKFLOW_SPECS[key]\n    if value.get("repository") != MAIN_QUALIFICATION_REPOSITORY:\n        raise Increment5ContractError(\n            "workflow attempt repository differs"\n        )\n    if value.get("repository_id") != MAIN_QUALIFICATION_REPOSITORY_ID:\n        raise Increment5ContractError(\n            "workflow attempt repository identity differs"\n        )\n    if value.get("event") != "push":\n        raise Increment5ContractError(\n            "workflow attempt is not a main push"\n        )\n    if value.get("ref") != MAIN_QUALIFICATION_REF:\n        raise Increment5ContractError(\n            "workflow attempt ref differs from main"\n        )\n    if value.get("head_branch") != MAIN_QUALIFICATION_BRANCH:\n        raise Increment5ContractError(\n            "workflow attempt branch differs from main"\n        )\n    if value.get("status") != "completed" or value.get("conclusion") != "success":\n        raise Increment5ContractError(\n            "workflow attempt did not complete successfully"\n        )\n    attempt = WorkflowAttemptEvidence(\n        key=key,\n        workflow_id=_require_positive_integer(\n            value.get("workflow_id"),\n            field=f"workflow_attempts.{key}.workflow_id",\n        ),\n        workflow_name=str(value.get("workflow_name")),\n        run_id=_require_positive_integer(\n            value.get("run_id"),\n            field=f"workflow_attempts.{key}.run_id",\n        ),\n        run_attempt=_require_positive_integer(\n            value.get("run_attempt"),\n            field=f"workflow_attempts.{key}.run_attempt",\n        ),\n        run_number=_require_positive_integer(\n            value.get("run_number"),\n            field=f"workflow_attempts.{key}.run_number",\n        ),\n        head_sha=_require_commit_sha(\n            value.get("head_sha"),\n            field=f"workflow_attempts.{key}.head_sha",\n        ),\n        head_tree_sha=_require_commit_sha(\n            value.get("head_tree_sha"),\n            field=f"workflow_attempts.{key}.head_tree_sha",\n        ),\n        workflow_sha=_require_commit_sha(\n            value.get("workflow_sha"),\n            field=f"workflow_attempts.{key}.workflow_sha",\n        ),\n        workflow_ref=str(value.get("workflow_ref")),\n        api_url=str(value.get("api_url")),\n        html_url=str(value.get("html_url")),\n        created_at=_parse_canonical_utc(\n            value.get("created_at"),\n            field=f"workflow_attempts.{key}.created_at",\n        ),\n        run_started_at=_parse_canonical_utc(\n            value.get("run_started_at"),\n            field=f"workflow_attempts.{key}.run_started_at",\n        ),\n        updated_at=_parse_canonical_utc(\n            value.get("updated_at"),\n            field=f"workflow_attempts.{key}.updated_at",\n        ),\n    )\n    if (attempt.workflow_id, attempt.workflow_name) != (\n        workflow_id,\n        workflow_name,\n    ):\n        raise Increment5ContractError(\n            "workflow attempt differs from permanent workflow"\n        )\n    if (\n        attempt.head_sha != qualified_main_commit_sha\n        or attempt.workflow_sha != qualified_main_commit_sha\n        or attempt.head_tree_sha != qualified_main_tree_sha\n    ):\n        raise Increment5ContractError(\n            "workflow attempt is not bound to qualified main"\n        )\n    return attempt\n\n\ndef _parse_workflow_attempts(\n    *,\n    value: Mapping[str, Any],\n    qualified_main_commit_sha: str,\n    qualified_main_tree_sha: str,\n) -> tuple[WorkflowAttemptEvidence, ...]:\n    attempts = tuple(\n        _parse_workflow_attempt(\n            key=key,\n            value=_require_mapping(\n                value[key],\n                field=f"workflow_attempts.{key}",\n            ),\n            qualified_main_commit_sha=qualified_main_commit_sha,\n            qualified_main_tree_sha=qualified_main_tree_sha,\n        )\n        for key in MAIN_QUALIFICATION_WORKFLOW_NAMES\n    )\n    identities = [item.identity for item in attempts]\n    if len(set(identities)) != len(identities):\n        raise Increment5ContractError(\n            "workflow attempt identities must be distinct"\n        )\n    return attempts\n\n\ndef _require_mapping(value: object, *, field: str) -> Mapping[str, Any]:\n    if not isinstance(value, Mapping):\n        raise Increment5ContractError(f"{field} must be an object")\n    return value\n\n\ndef _validate_decision_document(\n    *,\n    summary: Mapping[str, Any],\n    document: Mapping[str, Any],\n    qualified_main_commit_sha: str,\n    qualified_main_tree_sha: str,\n    sdlc_attempt: WorkflowAttemptEvidence,\n) -> tuple[str, str, int, int]:\n    document_digest = digest_bytes(canonical_json_bytes(document))\n    if summary.get("decision_document_digest") != document_digest:\n        raise Increment5ContractError(\n            "signed decision document digest differs"\n        )\n    if document.get("schema_version") != "newsroom.sdlc.shadow-decision.v1":\n        raise Increment5ContractError(\n            "signed decision schema version differs"\n        )\n    if document.get("result") != "PASS":\n        raise Increment5ContractError(\n            "signed decision result is not PASS"\n        )\n    if document.get("result_reason") != "PASS:decision":\n        raise Increment5ContractError(\n            "signed decision reason is not PASS"\n        )\n    if document.get("first_failure") is not None:\n        raise Increment5ContractError(\n            "PASS decision cannot retain a first failure"\n        )\n    decision_identity = _require_digest(\n        document.get("decision_identity"),\n        field="signed_decision.decision_identity",\n    )\n    if summary.get("decision_identity") != decision_identity:\n        raise Increment5ContractError(\n            "signed decision identity differs"\n        )\n\n    context = _require_mapping(\n        document.get("context"),\n        field="signed_decision.document.context",\n    )\n    event = _require_mapping(\n        document.get("event"),\n        field="signed_decision.document.event",\n    )\n    for field_name, expected in (\n        ("repository", MAIN_QUALIFICATION_REPOSITORY),\n        ("event_name", "push"),\n        ("ref", MAIN_QUALIFICATION_REF),\n        ("evaluated_sha", qualified_main_commit_sha),\n        ("evaluated_tree_sha", qualified_main_tree_sha),\n    ):\n        if context.get(field_name) != expected:\n            raise Increment5ContractError(\n                f"signed decision context {field_name} differs"\n            )\n        if event.get(field_name) != expected:\n            raise Increment5ContractError(\n                f"signed decision event {field_name} differs"\n            )\n    if context.get("workflow_sha") != qualified_main_commit_sha:\n        raise Increment5ContractError(\n            "signed decision workflow SHA differs from qualified main"\n        )\n    if context.get("run_id") != sdlc_attempt.run_id:\n        raise Increment5ContractError(\n            "signed decision run differs from SDLC attempt"\n        )\n    if context.get("run_attempt") != sdlc_attempt.run_attempt:\n        raise Increment5ContractError(\n            "signed decision attempt differs from SDLC attempt"\n        )\n    if context.get("workflow_ref") != sdlc_attempt.workflow_ref:\n        raise Increment5ContractError(\n            "signed decision workflow ref differs"\n        )\n\n    totals = _require_mapping(\n        document.get("totals"),\n        field="signed_decision.document.totals",\n    )\n    test_count = _require_positive_integer(\n        totals.get("test_count"),\n        field="signed_decision.test_count",\n    )\n    skip_count = _require_nonnegative_integer(\n        totals.get("skip_count"),\n        field="signed_decision.skip_count",\n    )\n    for field_name in (\n        "required_skip_count",\n        "failure_count",\n        "error_count",\n    ):\n        if totals.get(field_name) != 0:\n            raise Increment5ContractError(\n                f"signed decision {field_name} must be zero"\n            )\n    expected_summary = {\n        "workflow_run_id": sdlc_attempt.run_id,\n        "workflow_run_attempt": sdlc_attempt.run_attempt,\n        "decision_document_digest": document_digest,\n        "decision_identity": decision_identity,\n        "result": "PASS",\n        "result_reason": "PASS:decision",\n        "evaluated_sha": qualified_main_commit_sha,\n        "evaluated_tree_sha": qualified_main_tree_sha,\n        "test_count": test_count,\n        "skip_count": skip_count,\n        "required_skip_count": 0,\n        "failure_count": 0,\n        "error_count": 0,\n    }\n    if {\n        key: summary.get(key)\n        for key in expected_summary\n    } != expected_summary:\n        raise Increment5ContractError(\n            "signed decision summary differs from canonical artifact"\n        )\n\n    lanes = document.get("lanes")\n    if not isinstance(lanes, list) or not lanes:\n        raise Increment5ContractError(\n            "signed PASS decision has no retained lanes"\n        )\n    gate_count = 0\n    for lane in lanes:\n        lane_value = _require_mapping(\n            lane,\n            field="signed_decision.document.lane",\n        )\n        receipt = _require_mapping(\n            lane_value.get("receipt"),\n            field="signed_decision.document.lane.receipt",\n        )\n        gate_decisions = receipt.get("gate_decisions")\n        if not isinstance(gate_decisions, list):\n            raise Increment5ContractError(\n                "signed decision lane has no gate decisions"\n            )\n        for gate in gate_decisions:\n            gate_value = _require_mapping(\n                gate,\n                field="signed_decision.document.gate",\n            )\n            gate_count += 1\n            if gate_value.get("result") != "PASS":\n                raise Increment5ContractError(\n                    "signed PASS decision contains a non-PASS gate"\n                )\n    if gate_count == 0:\n        raise Increment5ContractError(\n            "signed PASS decision contains no gate evidence"\n        )\n    return document_digest, decision_identity, test_count, skip_count\n\n\ndef _signed_decision_value(\n    *,\n    document: Mapping[str, Any],\n    qualified_main_commit_sha: str,\n    qualified_main_tree_sha: str,\n    sdlc_attempt: WorkflowAttemptEvidence,\n) -> dict[str, object]:\n    document_copy = deepcopy(dict(document))\n    context = _require_mapping(\n        document_copy.get("context"),\n        field="signed_decision.document.context",\n    )\n    totals = _require_mapping(\n        document_copy.get("totals"),\n        field="signed_decision.document.totals",\n    )\n    summary: dict[str, object] = {\n        "workflow_run_id": sdlc_attempt.run_id,\n        "workflow_run_attempt": sdlc_attempt.run_attempt,\n        "decision_document_digest": digest_bytes(\n            canonical_json_bytes(document_copy)\n        ),\n        "decision_identity": document_copy.get("decision_identity"),\n        "result": document_copy.get("result"),\n        "result_reason": document_copy.get("result_reason"),\n        "evaluated_sha": context.get("evaluated_sha"),\n        "evaluated_tree_sha": context.get("evaluated_tree_sha"),\n        "test_count": totals.get("test_count"),\n        "skip_count": totals.get("skip_count"),\n        "required_skip_count": totals.get("required_skip_count"),\n        "failure_count": totals.get("failure_count"),\n        "error_count": totals.get("error_count"),\n        "decision_document": document_copy,\n    }\n    _validate_decision_document(\n        summary=summary,\n        document=document_copy,\n        qualified_main_commit_sha=qualified_main_commit_sha,\n        qualified_main_tree_sha=qualified_main_tree_sha,\n        sdlc_attempt=sdlc_attempt,\n    )\n    return summary\n\n\ndef main_qualification_record_value(\n    *,\n    proposal: Increment5ADecisionPacket,\n    approval_record_digest: str,\n    qualified_main_commit_sha: str,\n    qualified_main_tree_sha: str,\n    qualified_at: UtcTimestamp,\n    workflow_attempts: Mapping[str, Mapping[str, Any]],\n    decision_document: Mapping[str, Any],\n) -> dict[str, object]:\n    if not isinstance(proposal, Increment5ADecisionPacket):\n        raise Increment5ContractError(\n            "main qualification builder requires a typed proposal"\n        )\n    if not isinstance(qualified_at, UtcTimestamp):\n        raise Increment5ContractError(\n            "main qualification builder requires typed UTC time"\n        )\n    commit_sha = _require_commit_sha(\n        qualified_main_commit_sha,\n        field="qualified_main_commit_sha",\n    )\n    tree_sha = _require_commit_sha(\n        qualified_main_tree_sha,\n        field="qualified_main_tree_sha",\n    )\n    approval_digest = _require_digest(\n        approval_record_digest,\n        field="approval_record_digest",\n    )\n    if set(workflow_attempts) != set(MAIN_QUALIFICATION_WORKFLOW_NAMES):\n        raise Increment5ContractError(\n            "main qualification workflow inventory differs"\n        )\n    ordered_attempt_values = {\n        name: deepcopy(dict(workflow_attempts[name]))\n        for name in MAIN_QUALIFICATION_WORKFLOW_NAMES\n    }\n    parsed_attempts = _parse_workflow_attempts(\n        value=ordered_attempt_values,\n        qualified_main_commit_sha=commit_sha,\n        qualified_main_tree_sha=tree_sha,\n    )\n    sdlc_attempt = {\n        item.key: item for item in parsed_attempts\n    }["SDLC_EVIDENCE_SHADOW"]\n    signed_decision = _signed_decision_value(\n        document=decision_document,\n        qualified_main_commit_sha=commit_sha,\n        qualified_main_tree_sha=tree_sha,\n        sdlc_attempt=sdlc_attempt,\n    )\n    return {\n        "schema_version": MAIN_QUALIFICATION_RECORD_SCHEMA_VERSION,\n        "qualification_id": MAIN_QUALIFICATION_RECORD_ID,\n        "qualification_version": MAIN_QUALIFICATION_RECORD_VERSION,\n        "qualification_scope": MAIN_QUALIFICATION_SCOPE,\n        "qualification_effect": MAIN_QUALIFICATION_EFFECT,\n        "non_effects": list(MAIN_QUALIFICATION_NON_EFFECTS),\n        "repository": MAIN_QUALIFICATION_REPOSITORY,\n        "branch": MAIN_QUALIFICATION_BRANCH,\n        "issue_number": MAIN_QUALIFICATION_ISSUE_NUMBER,\n        "pull_request_number": MAIN_QUALIFICATION_PULL_REQUEST_NUMBER,\n        "qualified_main_commit_sha": commit_sha,\n        "qualified_main_tree_sha": tree_sha,\n        "qualified_main_commit_url": (\n            f"https://github.com/{MAIN_QUALIFICATION_REPOSITORY}/commit/"\n            f"{commit_sha}"\n        ),\n        "qualified_at": qualified_at.to_text(),\n        "approval_record_digest": approval_digest,\n        "proposal": {\n            "payload_digest": proposal.payload_digest,\n            "record_digest": proposal.record_digest,\n            "contract_bundle_digest": proposal.bundle.contract_digest,\n            "qualification_profile_schema_digest": (\n                _qualification_schema_digest()\n            ),\n            "main_qualification_record_schema_digest": (\n                MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST\n            ),\n        },\n        "workflow_attempts": ordered_attempt_values,\n        "signed_decision": signed_decision,\n    }\n\n\ndef load_increment5a_main_qualification_record(\n    path: Path,\n    *,\n    approval_record_digest: str,\n    proposal: Increment5ADecisionPacket = INCREMENT_5A_DECISION_PACKET,\n) -> Increment5AMainQualificationRecord:\n    expected_approval_digest = _require_digest(\n        approval_record_digest,\n        field="approval_record_digest",\n    )\n    try:\n        data = path.read_bytes()\n        value = json.loads(\n            data.decode("utf-8", errors="strict"),\n            object_pairs_hook=object_without_duplicate_names,\n        )\n    except (OSError, UnicodeError, json.JSONDecodeError) as exc:\n        raise Increment5ContractError(\n            f"cannot read Increment 5A main qualification record: {path}"\n        ) from exc\n    if not isinstance(value, dict):\n        raise Increment5ContractError(\n            "main qualification record root must be an object"\n        )\n    if data != canonical_json_bytes(value):\n        raise Increment5ContractError(\n            "main qualification record must be exact canonical JSON"\n        )\n    errors = _schema_errors(value)\n    if errors:\n        raise Increment5ContractError(\n            "main qualification record schema validation failed: " + errors[0]\n        )\n\n    commit_sha = _require_commit_sha(\n        value["qualified_main_commit_sha"],\n        field="qualified_main_commit_sha",\n    )\n    tree_sha = _require_commit_sha(\n        value["qualified_main_tree_sha"],\n        field="qualified_main_tree_sha",\n    )\n    expected_url = (\n        f"https://github.com/{MAIN_QUALIFICATION_REPOSITORY}/commit/"\n        f"{commit_sha}"\n    )\n    if value["qualified_main_commit_url"] != expected_url:\n        raise Increment5ContractError(\n            "main qualification commit URL differs"\n        )\n    if value["approval_record_digest"] != expected_approval_digest:\n        raise Increment5ContractError(\n            "main qualification approval digest differs"\n        )\n    proposal_value = _require_mapping(\n        value["proposal"],\n        field="main_qualification.proposal",\n    )\n    expected_proposal = {\n        "payload_digest": proposal.payload_digest,\n        "record_digest": proposal.record_digest,\n        "contract_bundle_digest": proposal.bundle.contract_digest,\n        "qualification_profile_schema_digest": (\n            _qualification_schema_digest()\n        ),\n        "main_qualification_record_schema_digest": (\n            MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST\n        ),\n    }\n    if dict(proposal_value) != expected_proposal:\n        raise Increment5ContractError(\n            "main qualification record does not bind the exact proposal"\n        )\n    attempts = _parse_workflow_attempts(\n        value=_require_mapping(\n            value["workflow_attempts"],\n            field="main_qualification.workflow_attempts",\n        ),\n        qualified_main_commit_sha=commit_sha,\n        qualified_main_tree_sha=tree_sha,\n    )\n    sdlc_attempt = {\n        item.key: item for item in attempts\n    }["SDLC_EVIDENCE_SHADOW"]\n    signed_decision = _require_mapping(\n        value["signed_decision"],\n        field="main_qualification.signed_decision",\n    )\n    decision_document = _require_mapping(\n        signed_decision["decision_document"],\n        field="main_qualification.signed_decision.decision_document",\n    )\n    (\n        decision_document_digest,\n        decision_identity,\n        test_count,\n        skip_count,\n    ) = _validate_decision_document(\n        summary=signed_decision,\n        document=decision_document,\n        qualified_main_commit_sha=commit_sha,\n        qualified_main_tree_sha=tree_sha,\n        sdlc_attempt=sdlc_attempt,\n    )\n    return Increment5AMainQualificationRecord(\n        qualified_main_commit_sha=commit_sha,\n        qualified_main_tree_sha=tree_sha,\n        qualified_at=_parse_canonical_utc(\n            value["qualified_at"],\n            field="qualified_at",\n        ),\n        approval_record_digest=expected_approval_digest,\n        proposal_payload_digest=proposal_value["payload_digest"],\n        proposal_record_digest=proposal_value["record_digest"],\n        proposal_contract_bundle_digest=proposal_value[\n            "contract_bundle_digest"\n        ],\n        qualification_profile_schema_digest=proposal_value[\n            "qualification_profile_schema_digest"\n        ],\n        main_qualification_record_schema_digest=proposal_value[\n            "main_qualification_record_schema_digest"\n        ],\n        workflow_attempts=attempts,\n        decision_document_digest=decision_document_digest,\n        decision_identity=decision_identity,\n        test_count=test_count,\n        skip_count=skip_count,\n        record_digest=digest_bytes(data),\n    )\n'
MAIN_TEST_CODE = 'from __future__ import annotations\n\nfrom copy import deepcopy\nimport json\nfrom pathlib import Path\n\nimport pytest\n\nimport newsroom.increment5.approval as approval_module\nfrom newsroom.authority.canonical import canonical_json_bytes, digest_bytes\nfrom newsroom.authority.types import UtcTimestamp\nfrom newsroom.increment5 import (\n    APPROVAL_EFFECT,\n    INCREMENT_5A_DECISION_AUTHORITY,\n    INCREMENT_5A_DECISION_PACKET,\n    Increment5ContractError,\n    Increment5ProfileError,\n    MAIN_QUALIFICATION_EFFECT,\n    MAIN_QUALIFICATION_NON_EFFECTS,\n    MAIN_QUALIFICATION_RECORD_DIGEST,\n    MAIN_QUALIFICATION_RECORD_PATH,\n    MAIN_QUALIFICATION_RECORD_SCHEMA,\n    MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST,\n    MAIN_QUALIFICATION_RECORD_SCHEMA_PATH,\n    MAIN_QUALIFICATION_REF,\n    MAIN_QUALIFICATION_REPOSITORY,\n    MAIN_QUALIFICATION_REPOSITORY_ID,\n    MAIN_QUALIFICATION_WORKFLOW_NAMES,\n    MAIN_QUALIFICATION_WORKFLOW_SPECS,\n    QUALIFICATION_PRODUCTION_PROFILE_SCHEMA_DIGEST,\n    RetrievalComponentKind,\n    RetrievalProfileKind,\n    approval_attestation_value,\n    expected_increment5a_owner_approval_body,\n    load_increment5a_approval_attestation,\n    load_increment5a_main_qualification_record,\n    main_qualification_record_value,\n    repository_main_qualification_record,\n)\n\n\n_APPROVAL_DIGEST = "sha256:" + "a" * 64\n_COMMIT_SHA = "1" * 40\n_TREE_SHA = "2" * 40\n_QUALIFIED_AT = UtcTimestamp.parse("2042-03-12T12:30:00.000000Z")\n_CREATED_AT = "2042-03-12T12:00:00.000000Z"\n_STARTED_AT = "2042-03-12T12:00:01.000000Z"\n_UPDATED_AT = "2042-03-12T12:20:00.000000Z"\n_DECISION_IDENTITY = "sha256:" + "b" * 64\n\n\ndef _workflow_attempts() -> dict[str, dict[str, object]]:\n    attempts: dict[str, dict[str, object]] = {}\n    for index, key in enumerate(MAIN_QUALIFICATION_WORKFLOW_NAMES, start=1):\n        workflow_id, workflow_name = MAIN_QUALIFICATION_WORKFLOW_SPECS[key]\n        run_id = 1000 + index\n        attempts[key] = {\n            "workflow_id": workflow_id,\n            "workflow_name": workflow_name,\n            "run_id": run_id,\n            "run_attempt": 1,\n            "run_number": 2000 + index,\n            "repository": MAIN_QUALIFICATION_REPOSITORY,\n            "repository_id": MAIN_QUALIFICATION_REPOSITORY_ID,\n            "event": "push",\n            "ref": MAIN_QUALIFICATION_REF,\n            "head_branch": "main",\n            "head_sha": _COMMIT_SHA,\n            "head_tree_sha": _TREE_SHA,\n            "workflow_sha": _COMMIT_SHA,\n            "workflow_ref": (\n                f"{MAIN_QUALIFICATION_REPOSITORY}/.github/workflows/"\n                f"{key.lower()}.yml@{MAIN_QUALIFICATION_REF}"\n            ),\n            "status": "completed",\n            "conclusion": "success",\n            "api_url": (\n                f"https://api.github.com/repos/"\n                f"{MAIN_QUALIFICATION_REPOSITORY}/actions/runs/{run_id}/"\n                "attempts/1"\n            ),\n            "html_url": (\n                f"https://github.com/{MAIN_QUALIFICATION_REPOSITORY}/"\n                f"actions/runs/{run_id}"\n            ),\n            "created_at": _CREATED_AT,\n            "run_started_at": _STARTED_AT,\n            "updated_at": _UPDATED_AT,\n        }\n    return attempts\n\n\ndef _decision_document(\n    *,\n    attempts: dict[str, dict[str, object]] | None = None,\n) -> dict[str, object]:\n    selected = _workflow_attempts() if attempts is None else attempts\n    sdlc = selected["SDLC_EVIDENCE_SHADOW"]\n    return {\n        "schema_version": "newsroom.sdlc.shadow-decision.v1",\n        "decision_identity": _DECISION_IDENTITY,\n        "result": "PASS",\n        "result_reason": "PASS:decision",\n        "first_failure": None,\n        "context": {\n            "repository": MAIN_QUALIFICATION_REPOSITORY,\n            "event_name": "push",\n            "ref": MAIN_QUALIFICATION_REF,\n            "evaluated_sha": _COMMIT_SHA,\n            "evaluated_tree_sha": _TREE_SHA,\n            "run_id": sdlc["run_id"],\n            "run_attempt": sdlc["run_attempt"],\n            "workflow_ref": sdlc["workflow_ref"],\n            "workflow_sha": _COMMIT_SHA,\n        },\n        "event": {\n            "repository": MAIN_QUALIFICATION_REPOSITORY,\n            "event_name": "push",\n            "ref": MAIN_QUALIFICATION_REF,\n            "evaluated_sha": _COMMIT_SHA,\n            "evaluated_tree_sha": _TREE_SHA,\n        },\n        "lanes": [\n            {\n                "lane_id": "core",\n                "receipt": {\n                    "gate_decisions": [\n                        {\n                            "gate_id": "core-deterministic",\n                            "result": "PASS",\n                        },\n                        {\n                            "gate_id": "source-integrity",\n                            "result": "PASS",\n                        },\n                    ]\n                },\n            }\n        ],\n        "totals": {\n            "test_count": 1920,\n            "skip_count": 38,\n            "required_skip_count": 0,\n            "failure_count": 0,\n            "error_count": 0,\n        },\n    }\n\n\ndef _value() -> dict[str, object]:\n    attempts = _workflow_attempts()\n    return main_qualification_record_value(\n        proposal=INCREMENT_5A_DECISION_PACKET,\n        approval_record_digest=_APPROVAL_DIGEST,\n        qualified_main_commit_sha=_COMMIT_SHA,\n        qualified_main_tree_sha=_TREE_SHA,\n        qualified_at=_QUALIFIED_AT,\n        workflow_attempts=attempts,\n        decision_document=_decision_document(attempts=attempts),\n    )\n\n\ndef _write(tmp_path: Path, value: dict[str, object]) -> Path:\n    path = tmp_path / "main-qualification.json"\n    path.write_bytes(canonical_json_bytes(value))\n    return path\n\n\ndef test_main_qualification_schema_artifact_is_canonical() -> None:\n    data = MAIN_QUALIFICATION_RECORD_SCHEMA_PATH.read_bytes()\n    assert data == canonical_json_bytes(json.loads(data.decode("utf-8")))\n    assert digest_bytes(data) == MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST\n    assert MAIN_QUALIFICATION_RECORD_SCHEMA["properties"]["branch"] == {\n        "const": "main"\n    }\n\n\ndef test_current_branch_has_no_post_merge_implementation_admission() -> None:\n    assert MAIN_QUALIFICATION_RECORD_DIGEST is None\n    assert not MAIN_QUALIFICATION_RECORD_PATH.exists()\n    assert repository_main_qualification_record() is None\n    assert not INCREMENT_5A_DECISION_AUTHORITY.production_authorized\n    assert not (\n        INCREMENT_5A_DECISION_AUTHORITY.downstream_implementation_authorized\n    )\n    assert INCREMENT_5A_DECISION_AUTHORITY.main_qualification_record_digest is None\n    assert INCREMENT_5A_DECISION_AUTHORITY.qualified_main_commit_sha is None\n\n\ndef test_main_qualification_record_binds_attempts_and_signed_pass(\n    tmp_path: Path,\n) -> None:\n    record = load_increment5a_main_qualification_record(\n        _write(tmp_path, _value()),\n        approval_record_digest=_APPROVAL_DIGEST,\n    )\n    assert record.qualified_main_commit_sha == _COMMIT_SHA\n    assert record.qualified_main_tree_sha == _TREE_SHA\n    assert record.qualified_at == _QUALIFIED_AT\n    assert record.approval_record_digest == _APPROVAL_DIGEST\n    assert tuple(record.workflow_attempt_by_name) == (\n        MAIN_QUALIFICATION_WORKFLOW_NAMES\n    )\n    assert record.decision_identity == _DECISION_IDENTITY\n    assert record.test_count == 1920\n    assert record.skip_count == 38\n\n\ndef test_main_qualification_record_rejects_wrong_approval(\n    tmp_path: Path,\n) -> None:\n    with pytest.raises(\n        Increment5ContractError,\n        match="approval digest differs",\n    ):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, _value()),\n            approval_record_digest="sha256:" + "c" * 64,\n        )\n\n\ndef test_main_qualification_record_rejects_wrong_proposal(\n    tmp_path: Path,\n) -> None:\n    value = _value()\n    proposal = value["proposal"]\n    assert isinstance(proposal, dict)\n    proposal["payload_digest"] = "sha256:" + "c" * 64\n    with pytest.raises(\n        Increment5ContractError,\n        match="does not bind the exact proposal",\n    ):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, value),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n\ndef test_main_qualification_record_rejects_incomplete_or_reused_attempts(\n    tmp_path: Path,\n) -> None:\n    missing = _value()\n    attempts = missing["workflow_attempts"]\n    assert isinstance(attempts, dict)\n    attempts.pop("CI")\n    with pytest.raises(\n        Increment5ContractError,\n        match="schema validation failed",\n    ):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, missing),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n    reused = _workflow_attempts()\n    reused["CI"]["run_id"] = reused["AUTHORITY_A2A"]["run_id"]\n    reused["CI"]["api_url"] = reused["AUTHORITY_A2A"]["api_url"]\n    reused["CI"]["html_url"] = reused["AUTHORITY_A2A"]["html_url"]\n    with pytest.raises(\n        Increment5ContractError,\n        match="identities must be distinct",\n    ):\n        main_qualification_record_value(\n            proposal=INCREMENT_5A_DECISION_PACKET,\n            approval_record_digest=_APPROVAL_DIGEST,\n            qualified_main_commit_sha=_COMMIT_SHA,\n            qualified_main_tree_sha=_TREE_SHA,\n            qualified_at=_QUALIFIED_AT,\n            workflow_attempts=reused,\n            decision_document=_decision_document(attempts=reused),\n        )\n\n\n@pytest.mark.parametrize(\n    ("field", "replacement", "message"),\n    (\n        ("head_sha", "3" * 40, "not bound to qualified main"),\n        ("head_tree_sha", "3" * 40, "not bound to qualified main"),\n        ("workflow_sha", "3" * 40, "not bound to qualified main"),\n        ("conclusion", "failure", "schema validation failed"),\n        ("event", "pull_request", "schema validation failed"),\n        ("ref", "refs/pull/255/merge", "schema validation failed"),\n    ),\n)\ndef test_workflow_attempts_must_be_successful_exact_main(\n    tmp_path: Path,\n    field: str,\n    replacement: str,\n    message: str,\n) -> None:\n    value = _value()\n    attempts = value["workflow_attempts"]\n    assert isinstance(attempts, dict)\n    ci = attempts["CI"]\n    assert isinstance(ci, dict)\n    ci[field] = replacement\n    with pytest.raises(Increment5ContractError, match=message):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, value),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n\ndef test_main_qualification_record_rejects_noncanonical_time(\n    tmp_path: Path,\n) -> None:\n    value = _value()\n    value["qualified_at"] = "2042-03-12T12:30:00Z"\n    with pytest.raises(\n        Increment5ContractError,\n        match="canonical UTC text",\n    ):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, value),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n\n@pytest.mark.parametrize(\n    ("field", "replacement", "message"),\n    (\n        ("result", "BUDGET_EXCEEDED", "result is not PASS"),\n        ("result_reason", "BUDGET_EXCEEDED:decision", "reason is not PASS"),\n        ("first_failure", {"gate_id": "core"}, "first failure"),\n    ),\n)\ndef test_signed_decision_must_be_pass(\n    tmp_path: Path,\n    field: str,\n    replacement: object,\n    message: str,\n) -> None:\n    value = _value()\n    signed = value["signed_decision"]\n    assert isinstance(signed, dict)\n    document = signed["decision_document"]\n    assert isinstance(document, dict)\n    document[field] = replacement\n    signed["decision_document_digest"] = digest_bytes(\n        canonical_json_bytes(document)\n    )\n    with pytest.raises(Increment5ContractError, match=message):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, value),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n\ndef test_signed_decision_summary_must_match_canonical_document(\n    tmp_path: Path,\n) -> None:\n    value = _value()\n    signed = value["signed_decision"]\n    assert isinstance(signed, dict)\n    signed["test_count"] = 1919\n    with pytest.raises(\n        Increment5ContractError,\n        match="summary differs",\n    ):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, value),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n\ndef test_signed_decision_document_digest_is_exact(\n    tmp_path: Path,\n) -> None:\n    value = _value()\n    signed = value["signed_decision"]\n    assert isinstance(signed, dict)\n    signed["decision_document_digest"] = "sha256:" + "c" * 64\n    with pytest.raises(\n        Increment5ContractError,\n        match="document digest differs",\n    ):\n        load_increment5a_main_qualification_record(\n            _write(tmp_path, value),\n            approval_record_digest=_APPROVAL_DIGEST,\n        )\n\n\ndef test_owner_approval_alone_cannot_open_increment5b(\n    tmp_path: Path,\n) -> None:\n    body_digest = digest_bytes(\n        expected_increment5a_owner_approval_body().encode("utf-8")\n    )\n    approval_path = tmp_path / "approval.json"\n    approval_path.write_bytes(\n        canonical_json_bytes(\n            approval_attestation_value(\n                proposal=INCREMENT_5A_DECISION_PACKET,\n                approved_at=_QUALIFIED_AT,\n                comment_id=123456789,\n                approval_comment_body_digest=body_digest,\n            )\n        )\n    )\n    approval = load_increment5a_approval_attestation(approval_path)\n    effective_digest_for = approval_module._effective_contract_digest_factory(\n        proposal=INCREMENT_5A_DECISION_PACKET,\n        qualification_schema_digest=(\n            QUALIFICATION_PRODUCTION_PROFILE_SCHEMA_DIGEST\n        ),\n        approval_effect=APPROVAL_EFFECT,\n    )\n    authority_type = approval_module._decision_authority_class_factory(\n        proposal=INCREMENT_5A_DECISION_PACKET,\n        load_approval=lambda: approval,\n        load_main_qualification=lambda: None,\n        effective_contract_digest_for=effective_digest_for,\n    )\n    authority = authority_type()\n\n    assert authority.production_qualification_authorized\n    assert not authority.production_authorized\n    assert not authority.downstream_implementation_authorized\n    assert authority.main_qualification_record_digest is None\n    assert authority.downstream_contract_digest is None\n    for kind in RetrievalComponentKind:\n        assert not authority.component_authorized(kind)\n    with pytest.raises(\n        Increment5ProfileError,\n        match="post-merge exact-main qualification record",\n    ):\n        authority.require_profile(RetrievalProfileKind.PRODUCTION)\n\n\ndef test_authority_effects_are_separate_and_exact() -> None:\n    assert APPROVAL_EFFECT == "PRODUCTION_EQUIVALENT_QUALIFICATION_ONLY"\n    assert MAIN_QUALIFICATION_EFFECT == (\n        "IMPLEMENTATION_OF_ISSUES_251_254_ONLY"\n    )\n    assert "DOWNSTREAM_IMPLEMENTATION" not in MAIN_QUALIFICATION_NON_EFFECTS\n\n\ndef test_workflow_inventory_is_exact() -> None:\n    assert MAIN_QUALIFICATION_WORKFLOW_NAMES == (\n        "CI",\n        "AUTHORITY_A2A",\n        "AUTHORITY_A2B",\n        "PROJECTION_B1",\n        "PROJECTION_B2_B3_C1_NEO4J",\n        "SDLC_EVIDENCE_SHADOW",\n    )\n'
DECISION_DOC_TEMPLATE = '# Increment 5A attempt-bound post-merge implementation admission\n\n**Issue:** #250\n**Pull request:** #255\n**Owner-record effect:** `{approval_effect}`\n**Main-admission effect:** `IMPLEMENTATION_OF_ISSUES_251_254_ONLY`\n**Approval schema:** `{approval_schema}`\n**Main-admission schema:** `{main_schema}`\n**Owner-body digest:** `{owner_body}`\n\nThe canonical owner record now authorizes production-equivalent qualification\nonly. `DOWNSTREAM_IMPLEMENTATION` is an immutable non-effect of that record.\n\nImplementation authority is created only by the separately pinned post-merge\nmain-qualification record. Each of the six permanent workflows is represented\nby one exact attempt with immutable workflow ID/name, run ID and attempt,\nrepository ID, push event, `refs/heads/main`, head commit/tree, workflow SHA/ref,\nsuccessful conclusion, exact API/HTML URLs and canonical timestamps. All\nattempts must be distinct and must bind the declared merged-main commit/tree.\n\nThe signed SDLC evidence is embedded as its exact canonical decision document.\nThe loader recomputes its document digest and requires `result=PASS`,\n`result_reason=PASS:decision`, no first failure, matching exact-main context and\nevent, the same SDLC run/attempt, zero failures/errors/required skips, matching\ntest/skip totals and only PASS gate decisions. The record summary cannot differ\nfrom the embedded canonical artifact.\n\nA failed, unrelated, pull-request, wrong-tree, reused-attempt, noncanonical,\nsummary-tampered or non-PASS record therefore cannot grant downstream\nimplementation authority.\n\nNo shadow, canary, production activation, publication, public effect,\nlive-source execution, external embedding API call, provider spending or\nprotected-content vector is authorized.\n'


def replace_once(source: str, old: str, new: str, *, label: str) -> str:
    count = source.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected one match, found {count}")
    return source.replace(old, new, 1)


def tracked_files_with(text: str) -> list[Path]:
    result = subprocess.run(
        ("git", "grep", "-l", "--fixed-strings", text, "--"),
        cwd=ROOT,
        check=False,
        capture_output=True,
        text=True,
    )
    if result.returncode not in (0, 1):
        raise SystemExit(result.stderr)
    return [ROOT / line for line in result.stdout.splitlines() if line]


def replace_in_tracked_files(old: str, new: str) -> None:
    for path in tracked_files_with(old):
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace(old, new), encoding="utf-8")


def main() -> None:
    main_path = ROOT / "newsroom/increment5/main_qualification.py"
    main_path.write_text(MAIN_CODE, encoding="utf-8")

    # Import once with the schema-artifact check intentionally disabled, then
    # materialise the exact canonical v2 artifact and enable the import check.
    from newsroom.increment5.main_qualification import (
        MAIN_QUALIFICATION_RECORD_SCHEMA,
        MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST,
        MAIN_QUALIFICATION_RECORD_SCHEMA_PATH,
    )
    from newsroom.authority.canonical import canonical_json_bytes

    MAIN_QUALIFICATION_RECORD_SCHEMA_PATH.write_bytes(
        canonical_json_bytes(MAIN_QUALIFICATION_RECORD_SCHEMA)
    )
    module_text = main_path.read_text(encoding="utf-8")
    module_text = replace_once(
        module_text,
        "# MATERIALIZE_SCHEMA_CHECK",
        "_require_schema_artifact()",
        label="main schema check",
    )
    main_path.write_text(module_text, encoding="utf-8")
    new_main_schema_digest = MAIN_QUALIFICATION_RECORD_SCHEMA_DIGEST

    old_main_artifact = (
        ROOT
        / "newsroom/increment5/data/"
        / "increment5a_main_qualification_record_v1.schema.json"
    )
    if old_main_artifact.exists():
        old_main_artifact.unlink()

    approval_path = ROOT / "newsroom/increment5/approval.py"
    approval = approval_path.read_text(encoding="utf-8")
    approval = replace_once(
        approval,
        'APPROVAL_EFFECT = "IMPLEMENTATION_AND_PRODUCTION_QUALIFICATION_ONLY"\n',
        f'APPROVAL_EFFECT = "{NEW_APPROVAL_EFFECT}"\n',
        label="approval effect",
    )
    approval = replace_once(
        approval,
        'APPROVAL_NON_EFFECTS = (\n    "CANARY",\n',
        (
            'APPROVAL_NON_EFFECTS = (\n'
            '    "CANARY",\n'
            '    "DOWNSTREAM_IMPLEMENTATION",\n'
        ),
        label="approval downstream non-effect",
    )
    approval = replace_once(
        approval,
        (
            '        "implementation and production-equivalent qualification '
            'contract for "\n'
            '        "issues #251–#254. This approval authorizes no shadow, '
            'canary, "\n'
        ),
        (
            '        "production-equivalent qualification contract for issues '
            '#251–#254. "\n'
            '        "This approval authorizes production-equivalent '
            'qualification only; "\n'
            '        "it authorizes no downstream implementation, shadow, '
            'canary, "\n'
        ),
        label="owner statement effect",
    )
    approval_path.write_text(approval, encoding="utf-8")

    approval_schema_path = (
        ROOT
        / "newsroom/increment5/data/"
        / "increment5a_owner_approval_attestation_v1.schema.json"
    )
    approval_schema = json.loads(
        approval_schema_path.read_text(encoding="utf-8")
    )
    approval_schema["properties"]["approval_effect"]["const"] = (
        NEW_APPROVAL_EFFECT
    )
    non_effects = approval_schema["properties"]["non_effects"]["const"]
    if "DOWNSTREAM_IMPLEMENTATION" not in non_effects:
        non_effects.insert(1, "DOWNSTREAM_IMPLEMENTATION")
    canonical_approval_schema = json.dumps(
        approval_schema,
        ensure_ascii=False,
        allow_nan=False,
        sort_keys=True,
        separators=(",", ":"),
    ).encode("utf-8")
    approval_schema_path.write_bytes(canonical_approval_schema)
    new_approval_schema_digest = (
        "sha256:" + hashlib.sha256(canonical_approval_schema).hexdigest()
    )

    owner_body = (
        "I approve Increment 5A proposal payload "
        "`sha256:4cc1de54a8d831358bbbe9b65c724d442401e84e3e72918df45807e140bdea56`, "
        "proposal record "
        "`sha256:77fe5544b33a85519b8c1fba57f41fe1a68aa411eb63c64be5429dbbd28ea913`, "
        "proposal contract bundle "
        "`sha256:c0976abd6c2d450f242351f2cd94b2589cac5e6a03f1a2f98bfe1acbcbc4ea8c`, "
        "effective production-qualification schema "
        "`sha256:3e2ac38b2c17d11b8ea29afe58bd0cdf6924146969600cad2b89fc82ec8607b9`, "
        "approval-attestation schema "
        f"`{new_approval_schema_digest}`, and fixture-replay schema "
        "`sha256:c7faf200a77ddb08fee48394594b85e985aecb9ad342b24cbbf6464bf38f387e` "
        "as the exact production-equivalent qualification contract for issues "
        "#251–#254. This approval authorizes production-equivalent qualification "
        "only; it authorizes no downstream implementation, shadow, canary, "
        "production activation, publication, public effect, live-source "
        "execution, external embedding API call, provider spending, or "
        "protected-content vector."
    )
    new_owner_body_digest = (
        "sha256:" + hashlib.sha256(owner_body.encode("utf-8")).hexdigest()
    )

    replace_in_tracked_files(
        OLD_APPROVAL_SCHEMA_DIGEST,
        new_approval_schema_digest,
    )
    replace_in_tracked_files(
        OLD_OWNER_BODY_DIGEST,
        new_owner_body_digest,
    )
    replace_in_tracked_files(
        OLD_MAIN_SCHEMA_DIGEST,
        new_main_schema_digest,
    )
    replace_in_tracked_files(
        "IMPLEMENTATION_AND_PRODUCTION_QUALIFICATION_ONLY",
        NEW_APPROVAL_EFFECT,
    )
    replace_in_tracked_files(
        "as the exact implementation and production-equivalent qualification contract",
        "as the exact production-equivalent qualification contract",
    )
    replace_in_tracked_files(
        "This approval authorizes no shadow, canary,",
        (
            "This approval authorizes production-equivalent qualification only; "
            "it authorizes no downstream implementation, shadow, canary,"
        ),
    )

    init_path = ROOT / "newsroom/increment5/__init__.py"
    init = init_path.read_text(encoding="utf-8")
    init = replace_once(
        init,
        "from .main_qualification import (\n    MAIN_QUALIFICATION_BRANCH,\n",
        (
            "from .main_qualification import (\n"
            "    MAIN_QUALIFICATION_BRANCH,\n"
            "    MAIN_QUALIFICATION_EFFECT,\n"
            "    MAIN_QUALIFICATION_NON_EFFECTS,\n"
        ),
        label="main effect package imports",
    )
    init = replace_once(
        init,
        (
            "    MAIN_QUALIFICATION_RECORD_VERSION,\n"
            "    MAIN_QUALIFICATION_SCOPE,\n"
            "    MAIN_QUALIFICATION_WORKFLOW_NAMES,\n"
        ),
        (
            "    MAIN_QUALIFICATION_RECORD_VERSION,\n"
            "    MAIN_QUALIFICATION_REF,\n"
            "    MAIN_QUALIFICATION_REPOSITORY,\n"
            "    MAIN_QUALIFICATION_REPOSITORY_ID,\n"
            "    MAIN_QUALIFICATION_SCOPE,\n"
            "    MAIN_QUALIFICATION_WORKFLOW_NAMES,\n"
            "    MAIN_QUALIFICATION_WORKFLOW_SPECS,\n"
        ),
        label="main evidence package imports",
    )
    init = replace_once(
        init,
        '    "MAIN_QUALIFICATION_BRANCH",\n    "MAIN_QUALIFICATION_RECORD_DIGEST",\n',
        (
            '    "MAIN_QUALIFICATION_BRANCH",\n'
            '    "MAIN_QUALIFICATION_EFFECT",\n'
            '    "MAIN_QUALIFICATION_NON_EFFECTS",\n'
            '    "MAIN_QUALIFICATION_RECORD_DIGEST",\n'
        ),
        label="main effect package all",
    )
    init = replace_once(
        init,
        (
            '    "MAIN_QUALIFICATION_RECORD_VERSION",\n'
            '    "MAIN_QUALIFICATION_SCOPE",\n'
            '    "MAIN_QUALIFICATION_WORKFLOW_NAMES",\n'
        ),
        (
            '    "MAIN_QUALIFICATION_RECORD_VERSION",\n'
            '    "MAIN_QUALIFICATION_REF",\n'
            '    "MAIN_QUALIFICATION_REPOSITORY",\n'
            '    "MAIN_QUALIFICATION_REPOSITORY_ID",\n'
            '    "MAIN_QUALIFICATION_SCOPE",\n'
            '    "MAIN_QUALIFICATION_WORKFLOW_NAMES",\n'
            '    "MAIN_QUALIFICATION_WORKFLOW_SPECS",\n'
        ),
        label="main evidence package all",
    )
    init_path.write_text(init, encoding="utf-8")

    main_test_path = (
        ROOT / "newsroom/tests/test_increment5a_main_qualification.py"
    )
    main_test_path.write_text(MAIN_TEST_CODE, encoding="utf-8")

    approval_test_path = ROOT / "newsroom/tests/test_increment5a_approval.py"
    approval_test = approval_test_path.read_text(encoding="utf-8")
    approval_test = replace_once(
        approval_test,
        (
            "    assert tuple(APPROVAL_NON_EFFECTS) == (\n"
            '        "CANARY",\n'
            '        "EXTERNAL_EMBEDDING_API_CALLS",\n'
        ),
        (
            "    assert tuple(APPROVAL_NON_EFFECTS) == (\n"
            '        "CANARY",\n'
            '        "DOWNSTREAM_IMPLEMENTATION",\n'
            '        "EXTERNAL_EMBEDDING_API_CALLS",\n'
        ),
        label="approval non-effect regression",
    )
    approval_test_path.write_text(approval_test, encoding="utf-8")

    documentation_replacements = (
        (
            "Materialisation authorises implementation and production-equivalent "
            "qualification only.",
            "Materialisation authorises production-equivalent qualification only; "
            "downstream implementation remains blocked until post-merge admission.",
        ),
        (
            "An admitted record authorizes only implementation of #251–#254 and "
            "production-equivalent qualification under the bound Plan.",
            "An admitted owner record authorizes production-equivalent "
            "qualification only. Implementation of #251–#254 remains blocked "
            "until the separately pinned post-merge admission record exists.",
        ),
        (
            "implementation plus production-equivalent qualification",
            "production-equivalent qualification only",
        ),
    )
    for old, new in documentation_replacements:
        replace_in_tracked_files(old, new)

    decision_doc = (
        ROOT
        / "docs/decisions/"
        / "2026-08-01-increment-5a-attempt-bound-main-admission.md"
    )
    decision_doc.write_text(
        DECISION_DOC_TEMPLATE.format(
            approval_effect=NEW_APPROVAL_EFFECT,
            approval_schema=new_approval_schema_digest,
            main_schema=new_main_schema_digest,
            owner_body=new_owner_body_digest,
        ),
        encoding="utf-8",
    )

    for stale in (
        OLD_APPROVAL_SCHEMA_DIGEST,
        OLD_OWNER_BODY_DIGEST,
        OLD_MAIN_SCHEMA_DIGEST,
        "IMPLEMENTATION_AND_PRODUCTION_QUALIFICATION_ONLY",
        "as the exact implementation and production-equivalent qualification contract",
    ):
        remaining = tracked_files_with(stale)
        if remaining:
            raise SystemExit(
                f"stale contract text {stale} remains in: "
                + ", ".join(str(path.relative_to(ROOT)) for path in remaining)
            )

    print(f"approval_schema={new_approval_schema_digest}")
    print(f"main_schema={new_main_schema_digest}")
    print(f"owner_body={new_owner_body_digest}")


if __name__ == "__main__":
    main()
