# Prompt system

The OpenClaw runner prompt registry, category prompt policy, Discord publisher
modes and planner templates are deleted. That stack is dead and must not be
restarted. Git history is the archive.

See [ADR 0009](docs/adr/0009-legacy-operational-newsroom-dead.md).
